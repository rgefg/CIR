# composed_image_retrieval subset

This repository contains a subset of the local `composed_image_retrieval` workspace:

- `model/`
- `src/`
- `third_party/open_clip/`

Exported from the working tree on 2026-03-18.
